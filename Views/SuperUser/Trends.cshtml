@using JobTracker.Models
@model JobTracker.Models.ViewModels.SuperUserTrendsViewModel

@{
    ViewData["Title"] = "Job Market Trends";
    var days = Model.Days;
    var label = days.HasValue && days.Value > 0 ? $"Last {days.Value} days" : "All time";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="mb-0">Job Market Trends</h2>
        <a asp-controller="SuperUser" asp-action="Index" class="btn btn-outline-secondary">Back</a>
    </div>

    <form method="get" class="row g-2 mb-4">
        <div class="col-md-3">
            <select name="days" class="form-select">
                <option value="" selected="@(days == null)">Default (90 days)</option>
                <option value="30" selected="@(days == 30)">Last 30 days</option>
                <option value="90" selected="@(days == 90)">Last 90 days</option>
                <option value="180" selected="@(days == 180)">Last 180 days</option>
                <option value="365" selected="@(days == 365)">Last 365 days</option>
                <option value="0" selected="@(days == 0)">All time</option>
            </select>
        </div>
        <div class="col-md-3 d-grid">
            <button type="submit" class="btn btn-primary">Apply</button>
        </div>
        <div class="col-md-6 d-flex align-items-center text-muted">
            <span>@label</span>
        </div>
    </form>

    <div class="row g-3 mb-4">
        <div class="col-12 col-md-4">
            <div class="card text-white bg-primary h-100 shadow-sm">
                <div class="card-body">
                    <div class="fw-semibold">Total Applications</div>
                    <div class="display-6 fw-bold mb-0">@Model.TotalApplications</div>
                    <div class="opacity-75 small">@label</div>
                </div>
            </div>
        </div>
        <div class="col-12 col-md-4">
            <div class="card text-white bg-success h-100 shadow-sm">
                <div class="card-body">
                    <div class="fw-semibold">Offers</div>
                    <div class="display-6 fw-bold mb-0">@Model.OfferCount</div>
                    <div class="opacity-75 small">@label</div>
                </div>
            </div>
        </div>
        <div class="col-12 col-md-4">
            <div class="card text-white bg-dark h-100 shadow-sm">
                <div class="card-body">
                    <div class="fw-semibold">Application Success Rate</div>
                    <div class="display-6 fw-bold mb-0">@Model.SuccessRate%</div>
                    <div class="opacity-75 small">Offers / total applications</div>
                </div>
            </div>
        </div>
    </div>

    <div class="row g-4">
        <div class="col-md-6">
            <div class="card h-100 shadow-sm">
                <div class="card-body p-0">
                    <div class="px-3 pt-3 pb-2">
                        <div class="fw-semibold">Most Applied Companies</div>
                        <div class="text-muted small">@label</div>
                    </div>
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Company</th>
                                    <th class="text-end">Applications</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.TopCompanies)
                                {
                                    <tr>
                                        <td>@item.Name</td>
                                        <td class="text-end">@item.Count</td>
                                    </tr>
                                }
                                @if (!Model.TopCompanies.Any())
                                {
                                    <tr>
                                        <td colspan="2" class="text-center py-4 text-muted">No data yet.</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card h-100 shadow-sm">
                <div class="card-body p-0">
                    <div class="px-3 pt-3 pb-2">
                        <div class="fw-semibold">Most Popular Job Titles</div>
                        <div class="text-muted small">@label</div>
                    </div>
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Title</th>
                                    <th class="text-end">Applications</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.TopJobTitles)
                                {
                                    <tr>
                                        <td>@item.Name</td>
                                        <td class="text-end">@item.Count</td>
                                    </tr>
                                }
                                @if (!Model.TopJobTitles.Any())
                                {
                                    <tr>
                                        <td colspan="2" class="text-center py-4 text-muted">No data yet.</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-body p-0">
                    <div class="px-3 pt-3 pb-2">
                        <div class="fw-semibold">Status Breakdown</div>
                        <div class="text-muted small">@label</div>
                    </div>
                    <div class="table-responsive">
                        <table class="table table-hover mb-0">
                            <thead class="table-light">
                                <tr>
                                    <th>Status</th>
                                    <th class="text-end">Count</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var row in Model.StatusBreakdown)
                                {
                                    <tr>
                                        <td>@row.Status</td>
                                        <td class="text-end">@row.Count</td>
                                    </tr>
                                }
                                @if (!Model.StatusBreakdown.Any())
                                {
                                    <tr>
                                        <td colspan="2" class="text-center py-4 text-muted">No data yet.</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
