@model JobTracker.Models.ViewModels.SuperUserUsersViewModel

@{
    ViewData["Title"] = "User Management";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="mb-0">Platform Statistics</h2>
    </div>

    <div class="row g-3 mb-4">
        <div class="col-6 col-md-3">
            <div class="card text-white bg-primary h-100 shadow-sm">
                <div class="card-body">
                    <div class="fw-semibold">Total Users</div>
                    <div class="display-5 fw-bold mb-0">@Model.Stats.TotalUsers</div>
                    <div class="opacity-75 small">Registered accounts</div>
                </div>
            </div>
        </div>
        <div class="col-6 col-md-3">
            <div class="card text-white bg-info h-100 shadow-sm">
                <div class="card-body">
                    <div class="fw-semibold">Total Applications</div>
                    <div class="display-5 fw-bold mb-0">@Model.Stats.TotalApplications</div>
                    <div class="opacity-75 small">All users combined</div>
                </div>
            </div>
        </div>
        <div class="col-6 col-md-3">
            <div class="card text-dark bg-warning h-100 shadow-sm">
                <div class="card-body">
                    <div class="fw-semibold">Total Interviews</div>
                    <div class="display-5 fw-bold mb-0">@Model.Stats.TotalInterviews</div>
                    <div class="opacity-75 small">Status = Interview</div>
                </div>
            </div>
        </div>
        <div class="col-6 col-md-3">
            <div class="card text-white bg-success h-100 shadow-sm">
                <div class="card-body">
                    <div class="fw-semibold">Total Offers</div>
                    <div class="display-5 fw-bold mb-0">@Model.Stats.TotalOffers</div>
                    <div class="opacity-75 small">Status = Offer</div>
                </div>
            </div>
        </div>
    </div>

    <div class="row g-3 mb-4">
        <div class="col-12 col-md-6">
            <div class="card h-100 shadow-sm">
                <div class="card-body">
                    <div class="fw-semibold">Applications This Month</div>
                    <div class="display-6 fw-bold mb-0">@Model.Stats.ApplicationsThisMonth</div>
                    <div class="text-muted small">@Model.Stats.CurrentMonthLabel</div>
                </div>
            </div>
        </div>
        <div class="col-12 col-md-6">
            <div class="card h-100 shadow-sm">
                <div class="card-body">
                    <div class="fw-semibold">Average Applications / User</div>
                    <div class="display-6 fw-bold mb-0">@Model.Stats.AverageApplicationsPerUser</div>
                    <div class="text-muted small">Total applications divided by users</div>
                </div>
            </div>
        </div>
    </div>

    <div class="card mb-4">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h4 class="mb-0">Most Active Users</h4>
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>User</th>
                            <th>Name</th>
                            <th class="text-end">Total Applications</th>
                        </tr>
                    </thead>
                    <tbody>
                    @foreach (var row in Model.MostActiveUsers)
                    {
                        <tr>
                            <td>@row.Email</td>
                            <td>@row.Name</td>
                            <td class="text-end">@row.TotalApplications</td>
                        </tr>
                    }
                    @if (Model.MostActiveUsers.Count == 0)
                    {
                        <tr>
                            <td colspan="3" class="text-center py-4">No data yet.</td>
                        </tr>
                    }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="mb-0">Users</h2>
    </div>

    <div class="table-responsive">
        <table class="table table-striped align-middle">
            <thead>
                <tr>
                    <th>Email</th>
                    <th>Name</th>
                    <th>Roles</th>
                    <th>Email Confirmed</th>
                    <th>Status</th>
                    <th class="text-end">Actions</th>
                </tr>
            </thead>
            <tbody>
            @foreach (var user in Model.Users)
            {
                <tr>
                    <td>@user.Email</td>
                    <td>@($"{user.FirstName} {user.LastName}".Trim())</td>
                    <td>@string.Join(", ", user.Roles)</td>
                    <td>@(user.EmailConfirmed ? "Yes" : "No")</td>
                    <td>
                        @if (user.IsDisabled)
                        {
                            <span class="badge bg-danger">Disabled</span>
                        }
                        else
                        {
                            <span class="badge bg-success">Active</span>
                        }
                    </td>
                    <td class="text-end">
                        <a asp-controller="SuperUser" asp-action="Activity" asp-route-id="@user.Id" class="btn btn-sm btn-outline-primary">Activity</a>

                        @if (user.IsDisabled)
                        {
                            <form asp-controller="SuperUser" asp-action="Enable" asp-route-id="@user.Id" method="post" class="d-inline">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-sm btn-outline-success">Enable</button>
                            </form>
                        }
                        else
                        {
                            <form asp-controller="SuperUser" asp-action="Disable" asp-route-id="@user.Id" method="post" class="d-inline">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-sm btn-outline-warning">Disable</button>
                            </form>
                        }

                        <form asp-controller="SuperUser" asp-action="Delete" asp-route-id="@user.Id" method="post" class="d-inline">
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                        </form>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>
